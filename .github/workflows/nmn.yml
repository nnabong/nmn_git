#nmn-test

name: nmn.yml

on: [push]

jobs:
  
  build:
    runs-on: [self-hosted, msg]

    steps:
    - uses: actions/checkout@v2
    - name: print_word
      run: |
       ./my_first_shell_in_github.ps1 -MsgTo nnabong@archgroup.com -MsgSubject "Test GitHub Email"
       echo "Nice!"